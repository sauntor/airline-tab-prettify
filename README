vim-airline-pretty
====
通过vim-plug等工具安装vim-airline并集成nerdtree、nerdtree-tabs、tagbar等插件后，默认的tabline显示出来的效果并不令人满意，例如，打开NERDTree，则它会被tabline显示成'NERD_tree_1'，同样tagbar被显示成'__Tagbar__'。如此，有洁癖的同学可能就觉得这样看着真别扭。这个插件就是解决这个问题的，默认情况下NERDtree会被显示成Explorer，tagbar则是Symbols。

